apiVersion: v1
kind: Service
metadata:
  name: azure-springboot-service
spec:
  type: NodePort
  selector:
    app: azure-springboot-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007
